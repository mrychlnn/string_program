# Input the users full name
full_name = input("Enter your full name: ")

# Count the characters
character_count = len(full_name)

# Print the number of character in the given full name
print("output:", character_count)
